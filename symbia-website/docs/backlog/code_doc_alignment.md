# Code/Doc Alignment Backlog — symbia-seed

- [high][config/security] Genesis verification not enforced at boot. Expected: daemon aborts if `${SYMBIA_STATE_DIR}/keys/genesis.json` is missing or fails sshsig verification (per security docs). Actual: after `./scripts/seed.sh reboot`, `/status` responds `ok:true` with `genesis_verify.ok=false` and services stay up. Repro: remove or omit `${SYMBIA_STATE_DIR}/keys/genesis.json`, start via `./scripts/seed.sh boot`, hit `http://127.0.0.1:8123/status`. Impact: provenance guarantees are not enforced; downstream components may assume verified identity when it is absent. Action: fail startup (or return 503 on health/status) when verification fails; add tests to guard.
- [medium][workers] Status worker logs repeated `AttributeError: 'dict' object has no attribute 'lower'` (seen in `symbia-seed.status.log` after `./scripts/seed.sh reboot`). Repro: boot stack, tail `${SYMBIA_STATE_DIR}/logs/symbia-seed.status.log` or call `/logs/file?path=symbia-seed.status.log`. Impact: status worker telemetry may be stale or incomplete; UI/docs that rely on it could drift. Action: harden `core/workers/status_aggregator.summarize` against non-string statuses; add regression test.
